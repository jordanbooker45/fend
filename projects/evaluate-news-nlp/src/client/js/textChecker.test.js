/* const textCheck = require("./textChecker");

describe("Form Validation with RegExp", () => {
  test("function should compare user input text to RegExp pattern", () => {
    let inptTxt = "";

    expect(textCheck(inptTxt)).toThrow(
      "Please enter at least 1 character and less than 280 characters!"
    );
  });
});
 */
