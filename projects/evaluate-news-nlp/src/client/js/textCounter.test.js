const textCountDown = require("./textCounter");

describe("Function should count characters typed", () => {
  test("Function should count characters typed", () => {
    expect(typeof textCheck).toBe("undefined");
  });
});
